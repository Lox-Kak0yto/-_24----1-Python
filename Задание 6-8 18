def find_longest_digit_sequence(text):
    max_length = 0
    current_length = 0
    
    for char in text:
        if char.isdigit():
            current_length += 1
            if current_length > max_length:
                max_length = current_length
        else:
            current_length = 0
    
    return max_length
