def count_russian_chars(s):
    count = 0
    for char in s:
        if 'а' <= char <= 'я' or 'А' <= char <= 'Я' or char == 'ё' or char == 'Ё':
            count += 1
    return count

text = "Ты дурак?"
print(count_russian_chars(text)) 
