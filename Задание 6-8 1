import re

def find_max_float(text):
    pattern = r'-?\d+\.\d+|-?\.\d+'
    matches = re.findall(pattern, text)
    if not matches:
        return None  
    floats = [float(num) for num in matches]
    return max(floats)
